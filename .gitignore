venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
.DS_Store
Thumbs.db

# Environment and secrets
.env
.env.*
!.env.example

# Build and packaging
build/
dist/
*.egg-info/
# Root-anchored on purpose. A bare `pyproject.toml` matches at every depth and
# silently withheld the sub-projects' own manifests (calculation_platform/,
# policy_comparator/), so a fresh clone could not `pip install -e` either one.
/pyproject.toml

# Logs and runtime data
*.log
data/logs/
.tmp/

# Server diagnostics (contains connection details)
server_check.sh
