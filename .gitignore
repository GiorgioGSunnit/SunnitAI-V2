venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
.DS_Store
Thumbs.db

# Environment and secrets
.env
.env.*
!.env.example

# Build and packaging
build/
dist/
*.egg-info/
# Root-anchored on purpose. A bare `pyproject.toml` matches at every depth and
# silently withheld this sub-project's own manifest, so a fresh clone could not
# run the documented `pip install -e "policy_comparator[dev]"`.
/pyproject.toml

# Logs and runtime data
*.log
data/logs/
.tmp/

# Server diagnostics (contains connection details)
server_check.sh
